name: Pages

on:
  schedule:
    - cron: '0 * * * *'
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  cache:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - path: stats-card.svg
            url: https://github-readme-stats.vercel.app/api?username=erik-donath
          - path: test.md
            url: https://raw.githubusercontent.com/Erik-Donath/Erik-Donath/main/README.md
    steps:
    - name: Create cache directory
      run: mkdir -p cache/

    - name: Cache files
      uses: actions/cache@v4
      with:
        path: cache/
        key: cache-${{ github.sha }}
        restore-keys: cache-

    - name: Fetch file
      run: |
        echo "Fetching ${{ matrix.url }} -> cache/${{ matrix.path }}"
        curl -v -f --max-time 30 "${{ matrix.url }}" > cache/${{ matrix.path }}.new &&
        mv cache/${{ matrix.path }}.new cache/${{ matrix.path }} &&
        echo "SUCCESS: $(wc -c < cache/${{ matrix.path }}) bytes"
      continue-on-error: true

    - name: Debug empty cache
      if: failure()
      run: |
        echo "curl failed - listing cache contents:"
        ls -la cache/ || echo "cache/ empty"
        curl -I "${{ matrix.url }}" || echo "Server returned: $?"

    - name: Upload cache
      uses: actions/upload-artifact@v4
      with:
        name: cache-${{ matrix.path }}
        path: cache/${{ matrix.path }}
        retention-days: 1

  page:
    runs-on: ubuntu-latest
    needs: cache
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        lfs: true
    
    - name: Download cache
      uses: actions/download-artifact@v4
      continue-on-error: true
      with:
        pattern: cache-*
        path: cache/
        
    - name: Setup Pages
      id: pages
      uses: actions/configure-pages@v5
      
    - name: Build with Jekyll
      uses: actions/jekyll-build-pages@v1
      with:
        source: ./
        destination: ./_site
        
    - name: Copy cache to site
      run: |
        mkdir -p _site/cache
        find cache -type f -exec cp {} _site/cache/ \; 2>/dev/null || echo "No files found"
        echo "Cache files in _site/cache/:"
        ls -la _site/cache/* 2>/dev/null || echo "None"

    - name: Upload Pages artifact
      uses: actions/upload-pages-artifact@v4
      with:
        path: ./_site

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: [page]
    steps:
    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4
